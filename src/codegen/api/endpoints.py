from codegen.api.modal import MODAL_WORKSPACE

RUN_CODEMOD_ENDPOINT = f"https://{MODAL_WORKSPACE}--cli-run-codemod.modal.run"
DOCS_ENDPOINT = f"https://{MODAL_WORKSPACE}--list-docs.modal.run"
SKILLS_ENDPOINT = f"https://{MODAL_WORKSPACE}--list-skills.modal.run"
